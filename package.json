{
  "name": "autocodex-write-test",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
